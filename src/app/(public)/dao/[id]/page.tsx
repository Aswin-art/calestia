import { Fragment } from "react";

export default function PageDetailDao({ params }: { params: { id: string } }) {
  return (
    <Fragment>
      <section className="container mx-auto px-4 py-28"></section>
    </Fragment>
  );
}
